// Goes through the game file heirarchy and loads actor information
// into the engine
public class CSLoader {
    CSEngine engine;

    public CSLoader(CSEngine engine) {
        this.engine = engine;
    }
}